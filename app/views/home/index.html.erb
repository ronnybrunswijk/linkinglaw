<div id="home-content">
 <input id="pageId" type="hidden" value="home">
 <%= render 'intro' %>
 <%= render 'advantages' %>
  <blockquote class="bg-black height150 centered">
    <span>Omdat elke ondernemer recht heeft op goed juridisch advies</span>
  </blockquote>
 <%= render 'about/the_industry' %>
 <%= render 'how_it_works' %>
 <%= render 'press' %>
</div>
