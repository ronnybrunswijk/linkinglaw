<div id="about-content">
  <input id="pageId" type="hidden" value="about">
  <%= render 'our_mission' %>
  <blockquote class="baby-blue">
    <span>Omdat elke ondernemer recht heeft op goed juridisch advies</span>
  </blockquote>
  <%= render 'the_industry' %>
  <div class="grey-ruler"></div>
  <%= render 'us' %>
</div>
