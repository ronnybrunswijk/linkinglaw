<div class="authform">
<%= form_for(@question, url: preview_questions_path) do |f| %>
  <h3><%= t(:ask_question, scope: [:questions]) %></h3>
  <% if @question.errors.any? %>
    <div id="error_explanation">
      <ul>
      <% @question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :title %><span class="char_count">(nog <span></span> tekens)</span><br>
    <%= f.text_field :title, autofocus: true, class: 'form-control', maxlength: 100 %>
  </div>
  <div class="form-group">
    <%= f.label :description %><span class="char_count">(nog <span></span> tekens)</span><br>
    <%= f.text_area :description, class: 'form-control', maxlength: 500 %>
  </div>
  <div class="actions">
    <%= f.submit t(:preview_question, scope: [:questions]), class: 'button right' %>
    <%= link_to t(:cancel, scope: [:labels]), home_path %>
  </div>
<% end %>
</div>
